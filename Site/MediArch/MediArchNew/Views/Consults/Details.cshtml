@model Data.Domain.Entities.Consult

@using MediArch.Data
@inject ApplicationDbContext users

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Consult</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
           Medic
        </dt>
        <dd>
            @users.Users.FirstOrDefault(x => new Guid(x.Id) == Model.MedicId).Email.ToString()
        </dd>
        <dt>
            Pacient
        </dt>
        <dd>
            @users.Users.FirstOrDefault(x => new Guid(x.Id) == Model.PacientId).Email.ToString()
        </dd>
        <dt>
            ConsultDate
        </dt>
        <dd>
            @Html.DisplayNameFor(model => model.ConsultDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Medicines)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Medicines)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ConsultResult)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ConsultResult)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Owner") || User.IsInRole("Moderator"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit | </a>
    }
    @if (User.IsInRole("Owner") || User.IsInRole("Moderator"))
    {
    <a asp-action="Index">Back to List</a>
    }
</div>
